services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: securelog
      POSTGRES_PASSWORD: securelog
      POSTGRES_DB: securelog
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:

